workflows:
  ios-swiftui-workflow:
    name: iOS SwiftUI Build
    max_build_duration: 30
    instance_type: mac_mini_m1
    scripts:
      - name: Initialize Project
        script: | 
          echo "Инициализация..."
      - name: Build
        script: | 
          echo "Сборка приложения kitanaizyxx..."
    artifacts:
      - "*.ipa"
